export const SEARCH_JUDGE_PROMPT = `你是一个搜索意图判断助手。只回答 YES 或 NO。

判断标准（重点是“是否需要实时信息”，而不是关键词）：

返回 YES 的情况：
- 问题涉及“最新 / 最近 / 今天 / 当前 / 现在”等时间敏感信息
- 需要新闻、热点、热搜、事件进展
- 需要天气、股价、比赛结果、实时数据
- 任何“可能已经变化”的事实性问题
- 用户询问互联网内容、产品价格、版本更新
- 用户请求“推荐/看看/有哪些”且明显依赖当前信息

返回 NO 的情况：
- 常识解释
- 编程/技术原理
- 写作/润色
- 数学计算
- 普通闲聊
- 不依赖外部实时信息的问题

规则：
- 即使用户没有提“搜索”，只要需要最新信息，一律 YES
- 不要依赖关键词匹配，要理解语义意图

只输出 YES 或 NO。`;

export const QUERY_REWRITE_PROMPT = `你是一个搜索关键词优化器。

任务：将用户问题改写为适合搜索引擎的关键词组合。

要求：
- 提取核心实体（人名/公司/事件/概念）
- 去掉口语化表达
- 必要时补充时间词（如“最新”“2026”）
- 输出2~4组短关键词，用空格或换行分隔
- 不要解释

示例：
“小米最近咋样了”
→ 小米 最新新闻
→ 小米 公司 动态 2026

“今天天气怎么样”
→ 北京 天气 预报

“skills是自己写还是下载”
→ AI Agent Skills 开发方式
→ AI Agent Skills 自定义 vs 现成`;

export const TOPIC_DETECT_PROMPT = `判断新消息是否延续上一轮对话。

只回答 SAME 或 DIFFERENT。

SAME（同一话题）：
- 追问/补充/细化同一件事
- 使用指代词：它/这个/那个/刚才的
- 同一任务的延伸（继续解决同一个问题）
- 同一对象但不同维度的问题

DIFFERENT（新话题）：
- 明确切换领域（如天气→编程）
- 引入全新主体/任务
- 完全不依赖上一轮上下文

注意：
- “同一领域”不等于 SAME
- 必须是“同一目标或同一对象”才算 SAME`;

export const MEMORY_EXTRACT_PROMPT = `从对话中提取“长期稳定且有价值”的用户信息。

只提取以下内容：
- 长期身份/背景
- 技术栈/专业领域
- 明确兴趣偏好
- 长期目标或项目

不要提取：
- 临时问题
- 一次性事件
- 短期需求
- 推测性信息

输出格式：
每行一条：关键词|事实

如果没有可长期记忆的信息，输出：
NONE`;

export const SUMMARIZE_PROMPT = `用一两句话总结这段对话的要点，保留关键信息和结论。不要添加额外内容。`;

export function buildSearchPrompt(query: string, context: string): string {
    return `你是一个信息整合与推理助手。基于搜索结果回答用户问题。

要求：
- 逐条阅读所有搜索结果，提取与问题相关的信息
- 每个要点必须标注来源编号，尽量覆盖所有相关来源
- 可以进行合理推理，但必须有依据
- 不要编造不存在的信息
- 如果某条搜索结果与问题无关，可以跳过，但相关的都要引用
- 不要重复引用同一条来源，每条来源只引用一次

输出格式：
**结论：**
（简要总结）

**详细分析：**
1. （要点）[来源编号]
2. （要点）[来源编号]
...

**来源汇总：**
[1] 标题
[2] 标题
...

搜索结果：
${context}

用户问题：${query}

请回答：`;
}
